name: Process C-priority price queue

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/process-c.yml"
  workflow_run:
    workflows: ["Refine B QC with line-level parsing and OCR"]
    types: [completed]

permissions:
  contents: write

concurrency:
  group: "regen-c-price-scan"
  cancel-in-progress: false

jobs:
  process:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'push' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 330
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Configure checkpoint commits
        run: |
          git config user.name "regen-med-collector"
          git config user.email "actions@users.noreply.github.com"

      - name: Process all C-priority plans
        shell: bash
        run: |
          set -e
          for chunk in $(seq 1 6); do
            echo "===== C chunk $chunk ====="
            git pull --rebase origin main
            python scripts/processor.py \
              --queue data/queue_c_v40.json \
              --state results/c_state.json \
              --results results/c_latest.json \
              --csv results/c_prices.csv \
              --index-cache results/index_cache.json \
              --max-items 24 \
              --workers 4

            git add results/c_state.json results/c_latest.json results/c_prices.csv || true
            if ! git diff --cached --quiet; then
              git commit -m "C collector checkpoint $chunk [skip ci]"
              git pull --rebase origin main
              git push
            fi

            PENDING=$(python - <<'PY'
          import json
          with open("results/c_state.json",encoding="utf-8") as f:
              print(json.load(f).get("pending",0))
          PY
            )
            echo "C pending=$PENDING"
            if [ "$PENDING" = "0" ]; then
              break
            fi
            sleep 5
          done
